<div class="container mx-auto p-8">
  <h1 class="text-4xl font-bold text-center mb-8">Liste des tâches</h1>

  <div class="text-right mb-4">
    <%= link_to "Nouvelle tâche", new_task_path, data: { turbo_frame: "new_task" }, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
  </div>

  <div id="tasks">
    <%= render @tasks %>
  </div>

  <%= turbo_frame_tag "new_task" do %>
    <!-- Le formulaire de création sera chargé ici -->
  <% end %>

</div>
