<div class="container mx-auto p-8">
  <h1 class="text-4xl font-bold text-center mb-8"><%= @task.title %></h1>

  <div class="bg-white shadow-md rounded p-6">
    <p class="text-lg mb-4"><strong>Description :</strong> <%= @task.description %></p>
    <p class="text-lg"><strong>Terminée :</strong> <%= @task.completed ? "Oui" : "Non" %></p>
  </div>

  <div class="mt-6 flex justify-between">
    <%= link_to "Modifier", edit_task_path(@task), class: "bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded" %>
    <%= link_to "Retour", tasks_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded" %>
  </div>
</div>
